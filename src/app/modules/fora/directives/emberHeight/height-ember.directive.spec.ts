import { EmberHeightDirective } from './height-ember.directive';

describe('HeightEmberDirective', () => {
  it('should create an instance', () => {
    const directive = new EmberHeightDirective();
    expect(directive).toBeTruthy();
  });
});
