import { HorizontalScrollDirective } from './horizontalscroll.directive';

describe('HorizontalscrollDirective', () => {
  it('should create an instance', () => {
    const directive = new HorizontalScrollDirective();
    expect(directive).toBeTruthy();
  });
});
